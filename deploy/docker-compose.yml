version: '3.8'

services:
  academic-studio:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: academic_outline_studio
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
